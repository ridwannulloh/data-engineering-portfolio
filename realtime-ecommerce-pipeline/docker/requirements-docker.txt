# Runtime dependencies for the containerized app services
# (producers, bronze-sink, batch pipelines).
#
# Intentionally a lean subset of ../requirements.txt — apache-flink (needs a
# JVM) and the test tooling are excluded since the app containers don't run
# the PyFlink jobs or pytest. Keep versions in sync with requirements.txt.

confluent-kafka==2.3.0
boto3==1.34.0
pyarrow==15.0.0
pandas==2.2.1
python-dotenv==1.0.1
